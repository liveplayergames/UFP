<?xml version="1.0" encoding="utf-8"?>
<resources>

    <dimen name="extra_space_under_card_area">2dp</dimen>
    <dimen name="poker_inter_button_space">1dp</dimen>

    <!-- this is margin above the header bar that introduces a batch of buttons.
        for example, "history" on the sendactivity. the margin is greater for large
        screens.
         -->
    <dimen name="header_margin_top">22dp</dimen>
    <dimen name="button_min_height">48dp</dimen>

    <!-- size of message text in poker activity -->
    <dimen name="poker_msg_size">24sp</dimen>
    <dimen name="poker_msg_min_height">40dp</dimen>

</resources>
